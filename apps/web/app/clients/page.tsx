import { ClientsPage } from "../../components/clients/ClientsPage";

export const metadata = {
  title: "Taskora · Clientes"
};

export default function ClientsRoute(): JSX.Element {
  return <ClientsPage />;
}
